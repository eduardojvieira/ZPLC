import { expect, test } from 'bun:test';
import { isElectronHeadlessBootstrapQuarantine } from './electronHeadlessQuarantine';
const details = [
  JSON.stringify({ method: 'Runtime.consoleAPICalled', human: 'Electron sandboxed_renderer.bundle.js script failed to run', context: JSON.stringify({ id: 2, name: 'Electron Isolated Context', isDefault: false, type: 'isolated', frameId: 'main' }), stack: [{ url: 'node:electron/js2c/sandbox_bundle' }] }),
  JSON.stringify({ method: 'Runtime.consoleAPICalled', human: "TypeError: Cannot destructure property 'preloadScripts' of 'binding.startupData' as it is null.\nstack", context: JSON.stringify({ id: 2, name: 'Electron Isolated Context', isDefault: false, type: 'isolated', frameId: 'main' }), stack: [{ url: 'node:electron/js2c/sandbox_bundle' }] }),
];
test('quarantines only Electron 43 headless CDP bootstrap pair', () => { expect(isElectronHeadlessBootstrapQuarantine('43.4.1', details, 'main')).toBe(true); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', [], 'main')).toBe(true); expect(isElectronHeadlessBootstrapQuarantine('43.4.2', details, 'main')).toBe(false); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', [...details, details[0]], 'main')).toBe(false); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', details, 'other')).toBe(false); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', [details[1], details[0]], 'main')).toBe(false); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', ['{', details[1]], 'main')).toBe(false); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', [details[0], JSON.stringify({ ...JSON.parse(details[1]), context: JSON.stringify({ id: 3, name: 'Electron Isolated Context', isDefault: false, type: 'isolated', frameId: 'main' }) })], 'main')).toBe(false); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', details.map((detail) => detail.replace('Runtime.consoleAPICalled', 'Log.entryAdded')), 'main')).toBe(false); expect(isElectronHeadlessBootstrapQuarantine('43.4.1', details.map((detail) => detail.replace('sandbox_bundle', 'other')), 'main')).toBe(false); });
