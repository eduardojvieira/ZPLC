{
  "name": "zplc",
  "private": true,
  "version": "2.0.0-rc.3",
  "description": "ZPLC - Open Source IEC 61131-3 PLC Runtime",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "overrides": {
    "webpack": "5.105.4"
  },
  "scripts": {
    "build": "bun run build:compiler && bun run build:ide",
    "test": "bun run test:compiler && bun run test:ide",
    "lint": "bun run lint:ide",
    "build:compiler": "bun run --cwd=packages/zplc-compiler build",
    "test:compiler": "bun run --cwd=packages/zplc-compiler test",
    "build:ide": "bun run --cwd=packages/zplc-ide build",
    "test:ide": "bun run --cwd=packages/zplc-ide test",
    "lint:ide": "bun run --cwd=packages/zplc-ide lint",
    "typecheck:hil": "bun run --cwd=packages/zplc-hil typecheck",
    "docs:generate": "bun run --cwd=docs generate:docs",
    "docs:validate": "bun run --cwd=docs validate:docs",
    "docs:build": "bun run --cwd=docs build",
    "check:versions": "bun scripts/release-version.ts --check",
    "check:typescript": "bun run build:compiler && bun run test:compiler && bun run typecheck:hil && bun run build:ide && bun run test:ide && bun run lint:ide",
    "check:docs": "bun run docs:generate && bun run docs:validate && bun run docs:build"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
